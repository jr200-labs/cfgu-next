# @jr200-labs/common

Configu common library.

## Overview

The `@jr200-labs/common` package contains shared logic for internal use by higher-level Configu interfaces. This package should not be installed directly or used as a standalone package.
